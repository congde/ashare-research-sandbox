--- a/app/src/todo_app/service.py
+++ b/app/src/todo_app/service.py
@@ -44,6 +44,10 @@ class TodoService:
         todo.done = True
         return todo
 
+    def delete(self, todo_id: int) -> None:
+        """删除 Todo —— 样本 Diff，未处理 id 不存在的情况。"""
+        del self._todos[todo_id]
+
     def sort_by_priority(self, todos: list[Todo]) -> list[Todo]:
         """按优先级降序排列（HIGH → MEDIUM → LOW）。"""
         return sorted(todos, key=lambda t: t.priority, reverse=True)
