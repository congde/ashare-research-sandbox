# 组织级 Codex 要求示例 —— 讲 16

[codex]
min_version = "0.20.0"

[forbidden_flags]
# 全员禁止绕过审批
list = ["--dangerously-bypass-approvals"]

[models]
allow = ["gpt-4.1", "o4-mini"]
default = "gpt-4.1"

[sandbox]
default_mode = "workspace-write"

[audit]
log_delegations = true
retention_days = 90
