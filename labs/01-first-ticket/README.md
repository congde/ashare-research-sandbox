# Lab 01: first ticket

This lab demonstrates a deliberately small hotfix. The starter implementation
sorts high-priority work after low-priority work. The test is the acceptance
contract and must not be changed to make the starter pass.

## Ticket

Read [ticket.md](ticket.md), then compare `starter/` with `solution/`.

## Verify the teaching fixture

```bash
make lab-01
```

Windows PowerShell:

```powershell
py scripts/course.py lab-01
```

The script proves two things:

1. The starter fails for the intended behavior.
2. The minimal solution passes the same test.

